#include "Quarto.h"

Quarto::Quarto(tipo_quarto tq, int p, int n, int c, float pb) {
    tquarto = tq;
    piso = p;
    numero = n;
    capacidade = c;
    preco_base = pb;
    promo = tquarto;
}